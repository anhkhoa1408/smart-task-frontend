import { InvalidTextDirective } from './invalid-text.directive';

describe('InvalidTextDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidTextDirective();
    expect(directive).toBeTruthy();
  });
});
